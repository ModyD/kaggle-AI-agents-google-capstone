# =============================================================================
# Root Environment Variables - Example Template
# =============================================================================
# This file contains shared environment variables used across the project.
# Copy to .env and fill in your values.
#
# Directory-specific env files:
#   - /backend/.env          - Backend-specific variables
#   - /frontend/.env         - Frontend-specific variables (Next.js)
#   - /notebooks/.env        - Jupyter notebook variables
#
# Priority: Directory-specific .env files override root .env values.

# =============================================================================
# Google Cloud Platform
# =============================================================================
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
VERTEX_AI_LOCATION=global  # Gemini models require global endpoint

# Service account credentials (local development only)
# In production, Cloud Run/Cloud Functions use workload identity
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# =============================================================================
# Vertex AI / Gemini
# =============================================================================
VERTEX_AI_MODEL=gemini-3-pro-preview
VERTEX_EMBEDDING_MODEL=text-embedding-004

# =============================================================================
# Neon PostgreSQL (pgvector)
# =============================================================================
# Get this from your Neon dashboard: https://console.neon.tech
NEON_DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# =============================================================================
# Upstash Redis
# =============================================================================
# Get these from your Upstash console: https://console.upstash.com
# Use the REST API URL (https://), not the Redis connection string (rediss://)
# Original format (for redis-py): rediss://default:your-token@your-endpoint.upstash.io:6379
UPSTASH_REDIS_REST_URL=https://your-endpoint.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# =============================================================================
# LangSmith (Optional - for LLM tracing)
# =============================================================================
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=security-agent

# =============================================================================
# Cloud Run Deployment
# =============================================================================
CLOUD_RUN_SERVICE_NAME=security-agent-backend
CLOUD_RUN_REGION=us-central1
CLOUD_RUN_MIN_INSTANCES=0
CLOUD_RUN_MAX_INSTANCES=2

# =============================================================================
# Frontend
# =============================================================================
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
